export default class WallModel {
    constructor(startX, startY, endX, endY, width, height) {
      this.startX = startX;
      this.startY = startY;
      this.endX = endX;
      this.endY = endY;
      this.width = width;
      this.height = height;
    }
  }
  